import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.checker.starter',
  appName: 'MY CHECKER',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
